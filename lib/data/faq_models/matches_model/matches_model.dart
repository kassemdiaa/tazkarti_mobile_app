import 'package:flutter/material.dart';

class MatchesModel extends StatelessWidget {
  const MatchesModel({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
